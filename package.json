{
  "name": "polylsp",
  "version": "0.1.0",
  "description": "A lightweight multi-language LSP client abstraction.",
  "license": "MIT",
  "type": "commonjs",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test tests/*.test.js"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "typescript": "^5.9.2"
  },
  "dependencies": {
    "@types/node": "^24.5.2",
    "typescript-language-server": "^5.3.3"
  }
}
